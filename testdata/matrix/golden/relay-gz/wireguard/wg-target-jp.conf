# 由 loom render 生成 —— 不要手工编辑(§12)
# 改配置请改 SSOT 再重新渲染;手工修改会被下次收敛覆盖(§15.3)
# 隧道 relay-gz → target-jp(wg),本端为发起方
# 发起方由两端 direction 推导:relay-gz=bidirectional, target-jp=direct_only(§2.2)

[Interface]
Address = 10.99.1.5/32
PostUp = wg set %i private-key /etc/loom/secrets/%i.key

[Peer]
# target-jp
PublicKey = jp0000000000000000000000000000000000000000k=
AllowedIPs = 10.99.1.6/32
Endpoint = 198.51.100.21:51823
PersistentKeepalive = 25
