# 由 loom render 生成 —— 不要手工编辑(§12)
# 改配置请改 SSOT 再重新渲染;手工修改会被下次收敛覆盖(§15.3)
# 隧道 target-sg → relay-cd(wg),本端为发起方
# 发起方由两端 direction 推导:target-sg=reverse_only, relay-cd=bidirectional(§2.2)

[Interface]
Address = 10.99.0.8/32
PostUp = wg set %i private-key /etc/loom/secrets/%i.key

[Peer]
# relay-cd
PublicKey = cd0000000000000000000000000000000000000000k=
AllowedIPs = 10.99.0.7/32
Endpoint = 203.0.113.13:51820
PersistentKeepalive = 25
