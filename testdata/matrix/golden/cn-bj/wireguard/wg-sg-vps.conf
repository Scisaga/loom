# 由 loom render 生成 —— 不要手工编辑(§12)
# 改配置请改 SSOT 再重新渲染;手工修改会被下次收敛覆盖(§15.3)
# 隧道 cn-bj ← sg-vps(wg),本端为接受方
# 发起方由两端 direction 推导:sg-vps=reverse_only, cn-bj=bidirectional(§2.2)

[Interface]
Address = 10.99.0.1/32
ListenPort = 51820
PostUp = wg set %i private-key /etc/loom/secrets/%i.key

[Peer]
# sg-vps
PublicKey = sg0000000000000000000000000000000000000000k=
AllowedIPs = 10.99.0.2/32
