# 由 loom render 生成 —— 不要手工编辑
# 改配置请改 SSOT 再重新渲染;手工修改会被下次收敛覆盖
# 隧道 cn-bj ← sg-vps(wg),本端为接受方
# 发起方由两端 direction 推导:sg-vps=reverse_only, cn-bj=bidirectional

[Interface]
Address = 10.99.0.1/32
ListenPort = 61637
PostUp = wg set %i private-key /etc/wireguard/node.key

[Peer]
# sg-vps
PublicKey = c2ctdnBzLXRlc3Qta2V5AAAAAAAAAAAAAAAAAAAAAAA=
AllowedIPs = 10.99.0.2/32
