# 由 loom render 生成 —— 不要手工编辑(§12)
# 改配置请改 SSOT 再重新渲染;手工修改会被下次收敛覆盖(§15.3)
# 隧道 sg-vps → cn-bj(wg),本端为发起方
# 发起方由两端 direction 推导:sg-vps=reverse_only, cn-bj=bidirectional(§2.2)

[Interface]
Address = 10.99.0.2/32
PostUp = wg set %i private-key /etc/wireguard/node.key

[Peer]
# cn-bj
PublicKey = Y24tYmotdGVzdC1rZXkAAAAAAAAAAAAAAAAAAAAAAAA=
AllowedIPs = 10.99.0.1/32
Endpoint = 203.0.113.10:61637
PersistentKeepalive = 25
