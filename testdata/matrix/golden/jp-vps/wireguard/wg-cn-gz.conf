# 由 loom render 生成 —— 不要手工编辑(§12)
# 改配置请改 SSOT 再重新渲染;手工修改会被下次收敛覆盖(§15.3)
# 隧道 jp-vps → cn-gz(wg),本端为发起方
# 发起方由两端 direction 推导:jp-vps=reverse_only, cn-gz=bidirectional(§2.2)

[Interface]
Address = 10.99.1.6/32
PostUp = wg set %i private-key /etc/loom/secrets/%i.key

[Peer]
# cn-gz
PublicKey = Y24tZ3otdGVzdC1rZXkAAAAAAAAAAAAAAAAAAAAAAAA=
AllowedIPs = 10.99.1.5/32
Endpoint = 203.0.113.12:61628
PersistentKeepalive = 25
