# 早期设计问题快照

[决策索引](../decisions.md) · [当前实现对照](../implementation.md)

**历史记录。** 本表来自早期决策文档，保留当时的未决项与取舍理由。它不是当前任务清单，
其中的单中控、密钥保管、平台完成情况及测量预算不能作为今天的规范或运行事实。

### 调度侧

| # | 问题 | 状态 | 答案 |
|---|---|---|---|
| 8 | ⚠️ **观测点**(§16.2) | **未定** | `ttft` / `tokens/s` 从哪来。**没有答案就不要选这两个目标函数** |
| 9 | 两级周期 | **部分已定** | `tuning_period` 已生效(本地部署参数不进版本库,§5.5.1)。`ranking_period` 仍未定 —— 现在只有一级 |
| 10 | 度量窗口 | **已定** | `window` / `min_samples` / `stale_after` 已生效,且三者的算术关系由校验器钉住([D24](../decisions/D024.md#d24--调参字段之间的算术要校验)) |
| 11 | 等价类校验频率与失败处理 | **未定** | |
| 12 | 探测预算 | **部分已定** | 一轮一条候选一个样本;按段量 + 剪枝把 15 条降到 6 条([D27](../decisions/D027.md#d27--按段量不按整条路线量))。硬上限仍未定 |
| 13 | 价格数据来源 | **未定** | 没有它 `objective: cost` 不可执行,Agent 会在渲染期拒绝 |

### 平台侧

| # | 问题 | 状态 | 答案 |
|---|---|---|---|
| 14 | 控制平面放在哪 | **已定，阶段一待部署** | 一棵签了名的静态树 + 一台中控([D32](../decisions/D032.md#d32--控制面是一棵签了名的静态树不需要被信任)、[D35](../decisions/D035.md#d35--中控只在改变系统时需要不在运行系统时需要)、[D88](../decisions/D088.md#d88--用签名-deployment-envelope-同时闭合反重放与真实-canary))。snapshot 内容签名 + signed current + 节点 floor 与高代 bootstrap 的带外 authority 锚定已实现；外部单调 witness 和自动 canary controller 尚未闭合。 |
| 15 | CA 私钥怎么保管 | **有意降级** | 5 台机器的规模上,§13.3 那套离线根 + 中间 CA + 人工解锁的成本远大于收益。当前:CA 与平台签名私钥都在中控上,0600,靠 `loom backup` 保存。**这是明知的取舍,不是遗漏**([D38](../decisions/D038.md#d38--离线根密钥当前有意不做)) |
| 16 | 秘密层怎么放 | **已定** | 每节点一份,只含它自己引用到的项(`loom secrets split`)。合并发生在节点上,分发树里只有占位符([D32](../decisions/D032.md#d32--控制面是一棵签了名的静态树不需要被信任))；两步轮换已实现(§13.4) |
| 17 | Android 客户端做到什么程度 | **未定** | |
| 18 | 是否面向多租户 | **未定** | |
| 19 | 是否启用指纹参数化 | **未定** | 影响 D1 与 D3 的优先级 |

> **#3 与 #8 仍然是硬阻塞。** #3(访问契约)卡住模式 B,#8(观测点)卡住
> `ttft` / `cost` 两个目标 —— Agent 现在会在渲染期拒绝它们
> 并说明原因,而不是拿 L4 首字节时间冒充(§5.5.1)。

---
