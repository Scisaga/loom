# 候选、调度与缺失数据

[文档地图](../../README.md) · [架构入口](../../design.md) · [控制面规范](../../distributed-control-plane.md) · [实现对照](../../implementation.md) · [本机部署信息](../../operations/local-deployment.md)

**规范范围：架构与数据平面。** 本文定义对应主题的规则；标明 v1 的契约仅用于该版本，标明目标态的机制不表示已实现。

---

## 5. 调度

### 5.1 策略裁剪候选集,度量在候选集内选优

> ### 不变量
> **度量不能取代策略。** 是否允许一个候选,是策略问题;在允许的候选中选哪个,才是度量问题。

```
所有可能 (路径 × 目标) 组合
     │  ← 策略过滤:方向约束、max_hops、等价类、
     │              合规约束(数据不出省 / 机房资质 / 备案状态)
     ▼
候选集
     │  ← 周期性评分:度量指标 + 声明属性(价格、配额)
     ▼
当前选中
```

**合规与安全是约束,不是评分项。** 数据驻留、机房资质、备案状态属于"允许/不允许",绝不能被高分挤掉。它们在过滤层生效,不进目标函数。

**先验网络知识同理。** ASN / 运营商归属可用于**裁剪候选集**(如"该接入点是电信,优先只探测电信出口的中继",减少探测组合数)和**排障标注**(标出某路径跨了哪些运营商),但**不进评分函数**。

> 理由:拓扑先验只能说明理论上该怎么走,实测直接说明实际上哪条更快。**两者冲突时永远信实测。** 一旦先验进入评分,它就会在某些场景下压过实测数据,违反本节不变量。
>
> 此外,真正决定国内跨网质量的是互联带宽与结算策略,这些没有公开数据源;公开可得的 BGP / AS 关系数据其更新尺度也与链路质量的变化尺度不匹配。**几十节点规模下,端到端实测的成本低于维护拓扑数据库,准确度还更高。**

### 5.2 评分输入分两类

| 类别 | 来源 | 特性 | 例子 |
|---|---|---|---|
| **度量值** | 探测与被动观测 | 高频变化、有噪声、需平滑 | RTT、丢包、抖动、TTFT、tokens/s |
| **声明值** | 合同、价格源、配置 | 低频变化、精确、有生效时间 | 单价、阶梯定价、配额、容量上限 |

> **两类输入性质不同,必须分开存储与处理。** 价格有版本、有生效时间、可能是阶梯的,不能塞进时序度量表。

### 5.3 目标函数:约束式优于加权求和

**"最优"不存在,只有"按什么最优"。**

| 目标 | 主导指标 | 适用 |
|---|---|---|
| `latency` | RTT p50 | 交互式请求、DNS |
| `ttft` | 建连开销 + 首包时间 | **流式推理** |
| `throughput` | 有效带宽、丢包率 | 大文件传输、备份 |
| `stability` | 抖动、断连率、握手成功率 | SSH、长连接 |
| `cost` | 单价 × 预估用量 | 成本敏感的批量任务 |

`ttft` 值得单列:流式推理体验由首字延迟主导,它对 RTT 和建连开销敏感、对带宽几乎不敏感。用 `throughput` 或 `latency` 去优化它都会选错。

> ⚠️ **但 `ttft` 与 `cost` 都不是 L4 能自己测出来的。** 选这两个目标,等于要求该等价类存在 L7 观测点或价格数据源([§16.2](measurement.md#162-被动观测优先但被动能看到什么由观测点决定)、[§5.2](#52-评分输入分两类))。校验器应拒绝"目标函数要的量根本没人产出"的声明。

**对外表达用约束 + 单一优化目标,不用加权求和:**

```
✅  在 华东 且 p95延迟 < 200ms 的端点中,选成本最低的
❌  score = 0.4×成本 + 0.6×延迟
```

客户能说清前者,给不出后者的权重。内部实现可以用加权启发式,但**暴露给用户的接口应该是约束式的**。

### 5.4 当前用启发式,后续引入求解器

**当前阶段:启发式。** 按约束过滤 → 按单一目标排序 → 取最优。

理由不是"先凑合",而是**现阶段的调度不是组合优化问题**:每个访问声明**独立地**从候选集里取最优,候选数在几十到几百量级,可枚举。对可枚举集合求 argmin,不需要搜索。

**后续引入求解器的触发条件是:决策之间产生耦合。** 典型来源是地址侧容量配额([§5.7](#57-跨客户聚合是网络效应))、服务器带宽约束、阶梯定价、多租户全局最优 —— 任一出现,问题就从 N 个独立 argmin 变成带约束的全局分配,启发式不再够用。

> 求解器届时作为**独立服务**接入,只服务于排序计算,**不进入每连接决策路径**([§5.6](#56-决策位置由信息可得性决定) 的本地决策不因此松动)。具体选型等触发时再定。

> **在没有度量数据之前写优化器,权重只能靠猜,且无法验证是否有效。** 这也是 [§20](../../design.md#20-依赖驱动的构建顺序) 中 L2 必须早于 L3 的原因。

### 5.5 调整周期与阻尼

> **"实时"应理解为"持续测量、周期决策",而不是"立即切换"。**

**主机制:固定调整周期。** 每个周期(如 10 分钟)重新评估一次,周期内不切换。这让系统行为可预测、可观察,并把切换频率的上界锁死。

**但周期只约束频率,不消除振荡** —— A/B 两条路径每 10 分钟交替仍是每天 144 次切换。因此最小组合是两件:

| 机制 | 作用 |
|---|---|
| **调整周期** | 锁死切换频率上界,周期内稳定 |
| **切换阈值** | 新候选须优于当前一个幅度(如 20%)才切,消除来回 |

后续可加:平滑窗口(移动中位数而非瞬时值)、切换代价项、熔断(单位时间切换超限则锁定并告警)。

**两个周期,对应 [§5.6](#56-决策位置由信息可得性决定) 的两个决策者:**

| 周期 | 谁执行 | 建议值 | 理由 |
|---|---|---|---|
| **排序周期** `ranking_period` | 控制平面重算并下发 ranked list | 30 分钟~小时级 | 换端点有结算、缓存、连接池代价;价格与配额本身就是低频量 |
| **微调周期** `tuning_period` | 接入节点在 top-N 内本地重选 | 5~10 分钟 | 链路质量变化尺度 |

> **两个周期必须分别配置**([§19](credentials-model.md#19-数据模型) 的 `AccessDeclaration` 各有一个字段)。合成一个值会强迫你在"端点切太频"和"链路反应太慢"之间二选一。**地址由请求决定时排序周期无意义**(地址轴是常量),只用微调周期。

### 5.6 决策位置由信息可得性决定

> **平台边界：** Windows/Android 的测量、证据复用与切换规则统一见
> [客户端观测复用](../../client-observation-reuse.md#客户端消费边界)。本节的服务级决策模型
> 不授权客户端运行 Linux Agent 的候选轮询或补样。

**这不是折中,是模式边界的自然结果。**

**排序的单位是完整路径候选,不是目标:**

```
RouteCandidate = (服务器链, 目标地址)
```

下发的 ranked list 是 **RouteCandidate 的有序表**,不是地址的有序表。理由见 [§4](model.md#4-两个选择轴):同一个地址经不同服务器链的质量互不相干。只下发全局地址排名,客户端会把"经广州最优的地址"错用在北京链路上。

| 地址轴 | 决策在哪 | 依据 |
|---|---|---|
| **由请求决定** | **接入节点本地,但按服务分别决定**([§4.5](model.md#45-服务中控分流与度量的单位)) | 地址不是我们的选择,**但它是决定该走哪条链的输入**;同一个服务内的地址共用一个决定 |
| **从等价类里选** | **控制平面算 RouteCandidate 排序 → 下发 ranked list;接入节点在 top-N 内按本地实测微调** | 价格、配额、容量、跨客户观测客户端拿不到;此刻链路的实际表现控制平面来不及感知 |

> ### 修正:这里曾经写着"地址是常量,只在服务器链上选"
>
> 下表是合成反例，只说明“不同目标的最优链可以不同”；目标和 probe 必须由 SSOT
> 显式指定，仓库不携带真实部署目标或测量。
>
> 那句话把**"地址不由我们选"**和**"地址不影响该选哪条链"**混为一谈了。
> 前者对,后者错得离谱。假设同一批候选得到以下结果:
>
> | 目标 | 经 edge-b | 经 cn-a | 直连 |
> |---|---|---|---|
> | `latency-a.example` | 1.400s | 0.140s | **0.060s** |
> | `latency-b.example` | 1.100s | 0.180s | **0.050s** |
> | `egress-check.example` | **0.400s** | 失败 | 失败 |
>
> **没有任何一条候选对三个目标都好。** 按原来的写法,`best-egress` 给整条
> 声明选一个候选（在这个合成例中会选中 edge-b），于是前两个目标明显变慢。
>
> 决策单位因此不是"声明",而是**"声明 × 服务"**([§4.5](model.md#45-服务中控分流与度量的单位))。

> **分工依据是信息可得性:控制平面掌握客户端无法观测的东西(价格、配额、他人观测),客户端掌握控制平面无法及时感知的东西(此刻这条链路的实际表现)。各自决定自己有信息优势的那部分。**

**接入节点选定整条 RouteCandidate,服务器不再做选择。**

服务器上的规则是 `凭据 → 访问声明 → 允许的下一跳集合`([§8.2](servers.md#82-凭据即访问声明)),它做的是**准入校验**而非选路。

> **为什么不让服务器选:** 服务器不知道这条连接属于哪个 RouteCandidate,也拿不到接入侧的实测数据。让它自主选下一跳,就有了两个互不知情的决策者,排序结果无法落地也无法归因。**选路的决策者必须只有一个。**

**机制就是普通的链式代理,没有额外发明。**

```
接入节点  --连接-->  服务器₁  --转发-->  服务器₂  --出去-->  目标地址
             ↑                  ↑                   ↑
        用凭据认证        下一跳是服务器₂        最后一跳直接出公网
```

接入节点建连时按顺序指明每一跳。每台服务器只做两件事:**校验这张凭据是否允许转发到请求的下一跳**,然后转发。

| 这一跳的下一跳是什么 | 服务器怎么处理 |
|---|---|
| 另一台服务器 | 走隧道转给它 |
| 目标地址(说明本机是出口) | 直接出公网 |

**因为目标不是节点([§1](model.md#1-两类节点加一类不是节点的东西)),最后一跳就是普通的对外连接** —— 不需要在目标那边装任何东西,也不需要为它维护配置。这正是"目标只是一个地址"这条不变量在数据平面上的体现。

`ranking_period` 正是控制平面重算排序并下发的周期([§5.5](#55-调整周期与阻尼))。**控制平面离线 → 客户端沿用上次 ranked list 继续在 top-N 内本地选择 → 降级的是排序新鲜度,不是选路能力**,[§11](control-rendering.md#11-铁律控制平面与数据平面分离) 铁律保住。

### 5.7 跨客户聚合是网络效应

服务调度场景下,来自不同接入点的测量数据聚合后,每条链路与每个地址的质量画像会显著更准。**客户越多,推荐越准,这是会随规模自我强化的部分。**

架构上要为它留位置:测量数据脱敏后跨租户聚合,聚合结果回流到排序。同时必须处理:

- 单个客户的测量不能被他人反推(脱敏与聚合阈值);
- 避免"羊群效应" —— 所有客户被导向同一个当前最优地址,反而把它压垮。**容量感知与分散策略是服务调度成熟后的必需项。**

### 5.8 数据不足与候选集为空

评分默认候选有数据。**三种情况下它没有,行为必须是确定的,不能留给实现随手决定。**

> 本节的窗口、`min_samples` 和受限主动补样只适用于 Linux/server Agent；
> Windows/Android 使用[客户端消费边界](../../client-observation-reuse.md#客户端消费边界)。

| 情况 | 定义 | 行为 |
|---|---|---|
| **冷启动** | Linux/server Agent 的新端点或新路径,样本数 < `min_samples` | **不参与排序,也不被淘汰。** 仅由该 Agent 在既有预算内主动探测；原生客户端保持 unknown |
| **数据过期** | 最新样本早于 `stale_after` | 视同无数据,退回冷启动状态。**过期数据绝不参与评分** —— 拿陈旧数据排序比不排序更糟 |
| **候选集为空** | 策略过滤或契约校验后无可用成员 | 见下 |

**Linux/server Agent 的度量窗口与样本数必须显式声明**,而不是"取最近的值":

| 参数 | 作用 | 建议 |
|---|---|---|
| `window` | 聚合窗口长度 | 与 `tuning_period` 同量级 |
| `min_samples` | 进入排序所需的最小样本数 | 不足则冷启动 |
| `stale_after` | 超过则视同无数据 | 约 3 个 `tuning_period` |

**候选集为空时,`fallback` 是访问声明上的显式字段,只有三个取值:**

| 取值 | 行为 | 适用 |
|---|---|---|
| **`fail_closed`(默认)** | 拒绝新连接并告警 | 有合规约束的声明 |
| `direct` | 回退到零跳直连 | 目标本身公网可达,且直连不违反策略 |
| `last_known_good` | 沿用最后一个可用候选,标记为降级并告警 | 目标轴稳定性优先于质量的场景 |

> **为什么默认 `fail_closed`:** [§5.1](#51-策略裁剪候选集度量在候选集内选优) 规定合规是约束不是评分项。若候选集因合规过滤而变空,任何"退而求其次"的自动回退都等于绕过约束。**空候选集是需要人介入的事件,不是需要自动兜底的事件。**

---

<a id="第二部分--数据平面"></a>

---
