# D40 · 装文件的那一步必须产出自检清单

[文档地图](../README.md) · [架构入口](../design.md) · [控制面规范](../distributed-control-plane.md) · [决策索引](../decisions.md)

**历史记录，非执行规范。** “生效”“当前”“已实现”“未定”等描述均属于记录当时；不代表当前规则、实现进度或部署事实。当前规则从文档地图按任务查找，不能按最新 D 编号覆盖规范正文。

---


**日期** 2026-08-23 · **状态** 生效

配置漂移自检靠一份"绝对路径 → 内容哈希"的清单。清单原本只有
`loom hydrate` 会写 —— 而节点自取根本不经过它。

后果:**每次 pull 之后,机器上的文件更新了而清单没有,自检永远误报"被改过"。**
实测五台机器同时报 `report/config.json` 漂移,而它们装的恰恰是刚发下来的
正确版本。

判据:**产出清单的必须是"最后决定文件内容"的那一步**,不能是旁边某个恰好
也知道内容的命令。现在 `loom pull` 自己算,`loom hydrate` 也算,两条路径
各自保证自己装的东西和清单一致。

这是 [D21](D021.md#d21--加字段之前先验证下游真的会用它) 的另一面:那条说"加字段前
先确认下游会读",这条说**加一条新路径时,先确认原路径上的每个副产物都还有人产出**。

---
