# 节点生命周期

[文档地图](../README.md) · [架构入口](README.md) · [控制面规范](../protocols/control-plane/README.md) · [实现对照](../development/implementation.md) · [本机部署信息](../operations/local-deployment.md)

**职责：控制面架构映射。** 本文解释全系统关系和标明的 v1 契约；v2 对象、认证、状态机与验收规则统一定义在控制面专题，本文不另设 wire schema。

---

## 节点生命周期:四个状态,加和删要对称

加一台机器和删一台机器**不是对称的**,而这个不对称很容易被忽略:

```
加:  写进 SSOT → 新机器 bootstrap → 它开始自取
删:  从 SSOT 划掉 → ……然后呢?
```

划掉之后,别人不再配到它的隧道 —— 但**它自己什么都不知道**。sing-box 继续
监听、继续用旧凭据接客。而那些凭据是**跨节点共享**的:删掉 edge-a 时,
`cred/access-a-sg` 和 `cred/access-a-eg` 还明文躺在它硬盘上。**一台没人管的出口。**

所以节点有四个状态,不是两个:

| 状态 | 配置 | 隧道 | 进候选 | 用在什么时候 |
|---|---|---|---|---|
| 正常 | ✅ | ✅ | ✅ | |
| **排空** `drain` | ✅ | ✅ | ❌ | 迁移期间:新旧并存,拿实测数据决定何时切 |
| **下线** `decommission` | ❌ | ❌ | ❌ | 它从签名过的快照里读到停机指令,自己停 |
| 移除 | — | — | — | 从 SSOT 删掉。**必须在下线之后** |

### 暂停纯接入设备的转发访问

纯 `use_loom` Device 另有可恢复的 `paused` 访问开关，直接在设备列表暂停/恢复。
它不适用于兼具转发、出口或控制职责的机器，也不替代上述下线事务。服务器应用签名
配置后不再接受该 Device 的数据面凭据；身份、授权、客户端配置及更新通道保留，
本机直连不受影响。恢复沿用原身份，不重新 Enrollment；参见
[Device 生命周期](device-lifecycle.md#二维码重发与本机身份丢失)。

### 排空:让流量走开,但别瞎

机器到期要换地方时,你需要一个中间状态:**不再往它上面导流,但配置和隧道
都留着**。没有它只有两个坏选择——留在候选里(流量还在走),或者直接删掉
(隧道没了,于是拿不到新旧对比的实测数据)。

排空之后隧道照旧,是**有意的**:排空期间你仍然要能观测它。

校验器拦两种误用:把某条声明钉死的出口排空了(那条声明会一个候选都没有,
流量被阻断),以及把所有能当出口的机器都排空了。

### 下线:一条经过认证的停机指令

`decommission: true` 的节点进入签名过的 manifest 的 `decommissioned` 名单。
它下次 `loom pull` 读到自己在里面,就停掉全部服务并禁用自启。

停机是可重试的收敛事务：逐个严格 `stop` / `disable` 并复核，任一失败都不写
成功标记；`loom-pull.timer` 永远最后停，因此前面的失败仍有下一轮自修复入口。
全部收敛后才写 `DECOMMISSIONED` 并进入 rollout 的成功终态
`decommissioned`。这个终态不推进配置的 `LastGood`。

**为什么不用"不在 manifest 里"当停机信号:那是歧义的。** 可能是被删了,也
可能是有人渲染时漏了一个节点。**对歧义信号采取不可逆动作是危险的** ——
所以缺席只报错、维持现状;停机要明说。

**秘密不自动销毁。** 销毁不可逆,而误签一次就全没了。它要停,不要自毁。

### 移除:两件机器管不了的事

从 SSOT 删掉之后,还有两件必须人做:

1. **轮换它持有过的凭据。** `cred/...` 是跨节点共享的,那台机器上还有明文。
   `loom status` 会把这台机器该轮换的凭据逐条列出来,连命令一起打。清单按
   **候选链是否经过它**算,不是按声明的 `allowed_servers` —— 允许用到不等于
   真配上了。
2. **从内部 CA 吊销它的证书。**

### 处在过渡态,是看不出来的

排空和下线都是**过渡态**,都不该长期停在那儿 —— 而停在那儿没有任何症状:
机器还在跑、隧道还通、校验也过。所以 `loom status` 把不在"正常"状态的节点
单独列一段,并且直接写出下一步该做什么:排空之后要**同时**标下线并删掉引用
它的隧道(校验器要求这两件一起做),下线之后才轮到删节点本身和轮换凭据。

### control capability 必须先退出 FinalControlSet

普通 `drain/decommission/remove` 不得隐式改变控制成员。兼任 control 的 Device 退役时，
先加入并追平替代 learner（若需要），再由 old/new joint quorum 提交新 epoch、撤销旧 control
证书与投票 key；完成后才按本节处理它的 server/access 生命周期。最后一个 control member
不能用普通 Remove 删除。旧 quorum 已不可恢复时只能走离线 recovery epoch。

新增 Device 只允许由已入网管理员经 overlay-only `control_api`、internal service certificate
和 admin mTLS 向分布式控制平面提交 Create Device，再由客户端导入二维码/加入文件、建立
短期受限 bootstrap tunnel 并访问私有 Enrollment
完成绑定；目标产品/API 不提供 `loom addnode` 或控制台 SSH Add node 旁路，legacy 入口是否仍
存在只见[实现对照](../development/implementation.md)。隧道地址和端口仍由加入事务中的分配器
计算，不由用户手填。

v1 契约允许凭据轮换和 CA 吊销采用人工运维。目标态把二者作为显式、可重试的 certified
operation，由受租约 executor 执行并读回；这不等于“删除节点时顺手销毁秘密”。UI 必须
持续展示未完成项，因为忘掉它们不会有任何症状，直到旧机器上的凭据被重新使用。
